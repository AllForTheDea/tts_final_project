<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @project.title %>
</p>

<%= link_to 'Edit', edit_project_path(@project) %> 


<% @project.characterlines.each do |post| %>
	<div class="container-fluid" style = "border-color: black;">
		<div class="well" style = "border-color: black;">
		   <span style = "font-weight: bold;"><%= post.character %>:</span> <%= post.line %>
		</div>
	</div>
	
<%end%>
<br />

<%= render 'characterlines/form', characterline: @characterline %>
<%= link_to 'Back', myprojects_path %>



